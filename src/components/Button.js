import React from "react";
import "../css/components/button.scss";

const Button = (name, mainColor, secColor) => {
  return (
    <>
      <a className="btn btn-lg">What have I built?</a>
    </>
  );
};

export default Button;
